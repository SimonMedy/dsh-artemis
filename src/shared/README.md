# Shared protocol

JSON-safe DTOs, RPC method names and state models shared by Host and Client.

This module must remain independent from Node/browser-specific implementations and from raw Cordis/ARTEMIS internal objects.

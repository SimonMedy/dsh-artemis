name: Temporary strict evidence step numbers

on:
  push:
    branches: [gpt/phase-7-strict-evidence-step-number]
    paths:
      - .github/workflows/temp-harden-evidence-step-number.yml
      - scripts/temp-harden-evidence-step-number.mjs

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          persist-credentials: false
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22.20.0
      - name: Apply and validate strict evidence step numbers
        run: |
          node scripts/temp-harden-evidence-step-number.mjs
          git diff --check
          npm run check
      - name: Commit validated permanent changes
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH: ${{ github.ref_name }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/host/artemis-evidence.mjs test/evidence.test.mjs test/trace-evidence.test.mjs docs/trace-evidence.md
          git commit -m "fix: reject malformed ARTEMIS step numbers"
          AUTH=$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)
          git -c http.extraHeader="AUTHORIZATION: basic $AUTH" push origin HEAD:"$BRANCH"
